
/* Lesson 94 フォーム入力バインディング基本
const app = Vue.createApp({
    data: () => ({
        message: 'Hello Vue.js'
    }),
})
app.mount('#app')
*/

/* Lesson 95 v-model利用時は属性無視
const app = Vue.createApp({
    data: () => ({
        message: 'Hello Vue.js'
    }),
})
app.mount('#app')
*/

/* Lesson 96 複数行テキスト
const app = Vue.createApp({
    data: () => ({
        message: 'Hello Vue.js'
    }),
})
app.mount('#app')
*/

/* Lesson 98 チェックボックス（単体）
const app = Vue.createApp({
    data: () => ({
        checked: false
    })
})
app.mount('#app')
*/

/* Lesson 99 チェックボックス（複数）
const app = Vue.createApp({
    data: () => ({
        colors: []
    }),
})
app.mount('#app')
*/

/* Lesson 100 ラジオボタン
const app = Vue.createApp({
    data: () => ({
        color: ''
    }),
})
app.mount('#app')
*/

/* Lesson 101 セレクトボックス（単体）
const app = Vue.createApp({
    data: () => ({
        selected: ''
    }),
})
app.mount('#app')
*/

/* Lesson 102 セレクトボックス（複数）
const app = Vue.createApp({
    data: () => ({
        selected: []
    }),
})
app.mount('#app')
*/

/* Lesson 104 修飾子.lazy
const app = Vue.createApp({
    data: () => ({
        message: ''
    }),
})
app.mount('#app')
*/

/* Lesson 105 修飾子.trim
const app = Vue.createApp({
    data: () => ({
        message: ''
    }),
})
app.mount('#app')
*/

/* Lesson 106 修飾子.number
const app = Vue.createApp({
    data: () => ({
        age: 0
    }),
})
app.mount('#app')
*/
